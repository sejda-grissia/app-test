const express = require('express');
const services= require('./routes/api/services.js');
const app = express();
const port = process.env.PORT || 8000;
app.listen(port,"0.0.0.0", ()=>{
  console.log(`server is running on ${port}`)
});
app.use('/api/services', services);

